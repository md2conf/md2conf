package io.github.md2conf.toolset.config;

import java.nio.file.Path;

public class ConfigInitializer {

    //todo read, write, merge, create .m2conf dir


    void init(Path inputDir, Path outputDir) {
        if (outputDir==null) {
// todo           outputDir = inputDir.normalize() + ".out";
        }
        //todo
    }


}
